[uwsgi]

chdir = /opt/ceeq

module = ceeq.wsgi

env=DJANGO_SETTINGS_MODULE=ceeq.settings.qa

home = /opt/ceeq/env

master = true

processes = 3

socket = /opt/ceeq/run/ceeq.sock

chmod-socket = 666

vacuum = true
